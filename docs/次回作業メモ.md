# 次回作業メモ

最終更新: 2026-02-25

## 次回優先タスク

1. UI最終調整（継続）
- 社員画面 / 確認画面 / 管理画面の文言・余白の最終微調整。
- モバイル幅でのボタン並び・テーブル可読性を実機目視で確認。
- `キャンセル` ボタン（`/` 遷移）を残すかどうか運用方針を確定。

2. 認証/入力バリデーション強化
- `sign-in` / `login` の氏名カナ入力バリデーション（カタカナ制約、trim/正規化）を強化。
- 表記ゆれ対策（全角/半角スペース、長音など）の方針決定。

3. テスト整備
- 最低限の E2E シナリオ追加（sign-in, login, 申請, 確認, 提出, 管理者閲覧）。
- name-login / 管理者判定 / 提出保存の回帰テスト追加。

4. ドキュメント整備（継続）
- README に画面遷移図（簡易）を追加。
- `docs/07_deployment_flow.md` などに旧Google OAuth前提の記述が残っていないか確認・更新。
- 運用手順（バックアップ / 復旧 / ログ確認）を docs 化。

5. 運用準備
- 本番用 `.env` 項目チェックリストの明確化。
- 管理者交代手順（`PRESIDENT_EMAIL` 更新）の実運用手順化。

## 次回開始時チェック

- `npm run docker:local:up`
- `http://localhost:3001/api/v1/health` が `ok`
- `sign-in.html` / `login.html` の導線確認
- admin / employee のログイン確認
- `employee-confirm.html` の操作カードと下部ナビ配置確認
