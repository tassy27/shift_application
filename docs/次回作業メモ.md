# 次回作業メモ

最終更新: 2026-02-25

## 次回優先タスク

1. 認証仕様の確定（完了）
- 完了: `docs/10_auth_spec.md` に運用手順を明文化。
- 完了: `PRESIDENT_EMAIL` の変更手順・注意点を明文化。

2. 設定ファイルの整理（完了）
- 完了: `.env.example` から `RUNTIME_PARAMS_CSV_PATH` を削除。
- 完了: CSV同期スクリプトは「互換用ユーティリティとして維持（標準運用では不使用）」に決定。

3. UIの最終確認（進行中）
- 申請画面/確認画面の表示崩れがないか PC・モバイルで確認。
- 確認カレンダーの時刻表示レイアウト（改行・文字サイズ）を最終調整。

- 追加方針: 本番はローカルログイン廃止（OAuth前提）。実装は後続タスクで段階的に対応。

4. テスト整備
- 最低限の E2E シナリオ追加（ログイン、申請、管理者編集）。
- `employeeId` 型ゆらぎや role 判定の回帰テストを追加。

5. 運用準備
- README に「本番投入時チェックリスト」を追加。
- ログ確認手順（障害時の切り分け手順）を docs 化。

## 次回開始時チェック

- `npm run docker:local:up`
- `http://localhost:3001/api/v1/health` が `ok`
- ログイン (`/login.html`) で admin / employee の動作確認




