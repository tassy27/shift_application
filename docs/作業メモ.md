# 作業メモ

最終更新: 2026-02-20

## 本日の実施内容

1. 起動方式の整理
- `.env` を設定の正とする方針へ変更。
- `package.json` の `api:start` / `local:server` から CSV 同期実行を外した。
- `docker-compose.local.yml` を `.env` 読み込み前提へ変更。

2. バックエンド修正
- `src/backend/config.ts`: CSV 依存を外し `.env` からのみ読み込む構成に変更。
- `src/backend/auth.ts`: Google OAuth未設定時でもセッションシリアライザが動くよう修正。
- `src/backend/server.ts`: `employee-login` 時のロール判定を見直し、既存 admin の降格を防止。
- `src/backend/db.ts`: ブートストラップ時に `admin.demo@example.com` を admin として補正。
- `src/backend/routes/employee-routes.ts`: `employeeId` の型ゆらぎ（文字列/数値）を吸収して申請失敗を防止。

3. フロント修正
- `public/employee.html`
  - 平日一括チェックボタンを削除。
  - チェック時のデフォルト時間を `09:00-16:00` へ変更。
  - 備考欄を空欄初期化 + placeholder 追加。
- `public/employee-confirm.html`
  - カレンダー内に日別申請時間を表示するよう修正。
  - 既定時間表記を `09:00-16:00` に統一。

4. ドキュメント
- `README.md` を新規追加（セットアップ、起動、環境変数、トラブルシュート）。

5. GitHub反映
- リポジトリ初期化、初回コミット実施。
- `https://github.com/tassy27/shift_application.git` に `main` ブランチを push 済み。

## 現在の確認結果

- Docker起動: `npm run docker:local:up` 成功
- ヘルスチェック: `http://localhost:3001/api/v1/health` -> `200 OK`

## 追記（2026-02-25）
- docs/10_auth_spec.md を追加し、USE_AUTH=true 時の運用手順と PRESIDENT_EMAIL ルールを明文化。
- README.md から認証仕様ドキュメントへの参照を追加。


## 追記（2026-02-25 設定ファイル整理）
- .env.example から RUNTIME_PARAMS_CSV_PATH を削除。
- scripts/apply-migrations.js のエラーメッセージを .env 前提に更新。
- docs/07_deployment_flow.md と docs/09_local_server_equivalent.md を .env 前提へ更新。
- CSV同期スクリプト（`npm run env:sync`）は互換用ユーティリティとして維持する方針を明記。


## 追記（2026-02-25 認証方針・UI確認）
- 将来のレンタルサーバー運用を見据え、本番はローカルログインを使わず OAuth 前提とする方針を docs/10_auth_spec.md に反映。
- UI最終確認の一部として、確認カレンダーの時刻表示の可読性改善（public/css/app.css）を実施。




## 追記（2026-02-25 ローカル認証導線・UI再構成）
- Google OAuth を廃止し、`index -> sign-in -> login` の導線へ変更。
- 認証方式を `メールアドレス + 氏名（漢字） + 氏名（カナ）` の登録 / ログイン方式へ変更。
- `db/migrations/005_add_user_full_name_kana.sql` を追加し、`users.full_name_kana` を導入。
- `public/sign-in.html` を追加、`public/login.html` を新ログインUIへ再構成。
- `public/employee-home.html` を削除。
- `public/employee.html` / `public/employee-confirm.html` / `public/admin.html` の情報量を整理（サマリー削除、ボタン位置整理、確認画面の操作カード順序変更など）。
- 共通UI挙動として `public/js/app-common.js` に下部ナビ移動処理（デバッグ情報上 / アンカー指定対応）を追加。
- 管理者ダッシュボード上部カードの `対象月 / 選択中の提出 / 提出済み / 未提出` を削除。
- `main` にコミット・push 済み（`08dd0a3 Revamp local auth flow and refine shift UI`）。
